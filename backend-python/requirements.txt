# Voice Chat - Python Dependencies
# Install with: pip install -r requirements.txt

# Backend Server
websockets
python-dotenv

# Microsoft Agent Framework (same as .NET Microsoft.Agents.AI)
# The --pre flag is required while Agent Framework is in preview
# Install with: pip install -r requirements.txt --pre
# Provides AzureOpenAIChatClient in agent_framework.azure
agent-framework-core
agent-framework-azure-ai

# Azure Identity for authentication (for AzureCliCredential if needed)
azure-identity
azure-core

# UI Server (uses only standard library - no additional deps needed)
# http.server, socketserver, webbrowser are built-in
