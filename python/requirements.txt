# Voice Chat - Python Dependencies
# Install with: pip install -r requirements.txt

# Backend Server
websockets>=12.0
httpx>=0.27.0
python-dotenv>=1.0.0
openai>=1.0.0
azure-identity>=1.15.0

# UI Server (uses only standard library - no additional deps needed)
# http.server, socketserver, webbrowser are built-in
